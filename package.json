{
  "name": "Zero-Trust-Cyber-Security-Platform",
  "private": true,
  "scripts": {
    "postinstall": "cd project && npm install",
    "dev": "cd project && npm run dev",
    "build": "cd project && npm run build",
    "preview": "cd project && npm run preview"
  }
}